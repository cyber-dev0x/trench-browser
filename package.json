{
  "name": "trench-browser",
  "version": "0.9.1-alpha",
  "description": "Hunt signal, not noise — a browser UI concept with an AI signal engine",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "echo 'Static site — no build step required'"
  },
  "keywords": ["browser", "ai", "signal", "research"],
  "license": "MIT"
}
